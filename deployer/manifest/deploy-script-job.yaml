kind: Job
apiVersion: batch/v1
metadata:
  name: $name-custom-script-runner
spec:
  template:
    metadata:
      name: $name-custom-script
      labels:
        app.kubernetes.io/name: $name
    spec:
      serviceAccount: $serviceAccount
      containers:
      - name: $name-custom-script
#        image: gcr.io/launcher-poc-207208/elastifile-provisioner-deployer:latest
#        image: $registry/elastifile-provisioner-deployer:$tag
        image: $imageCustomScript
        command: ["/bin/bash"]
        args: ["-c", "/tmp/deploy.sh"]
        volumeMounts:
        - name: config-volume
          mountPath: /etc/config
      restartPolicy: Never
      volumes:
        - name: config-volume
          configMap:
            name: $name-deployer-config
  backoffLimit: 0
